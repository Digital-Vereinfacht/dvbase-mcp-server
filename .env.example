# ============================================
# DVBase MCP Server – Konfiguration
# ============================================
# Kopiere diese Datei nach .env und fülle die Werte aus:
#   cp .env.example .env

# --- Ninox API Zugang ---
# API Key aus Ninox: Zahnrad-Icon → Integrationen → Generate
NINOX_API_KEY=dein-ninox-api-key-hier

# Team ID: steht in der Ninox-URL nach /teams/
NINOX_TEAM_ID=deine-team-id

# Database ID: steht in der Ninox-URL nach /databases/
NINOX_DATABASE_ID=deine-database-id

# --- Sicherheit ---
# Diesen Token musst du auch in Claude.ai / Claude Desktop angeben.
# Generiere einen sicheren Token z.B. mit:  openssl rand -hex 32
MCP_AUTH_TOKEN=

# --- Optional ---
# Table-ID der Dokumentations-Tabelle (für Kontextwissen)
# Kann leer bleiben – dann funktioniert alles außer get_context
DOKU_TABLE_ID=

# Port auf dem der Server läuft (Standard: 3001)
PORT=3001
